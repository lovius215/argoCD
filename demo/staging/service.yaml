apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: staging
  labels:
    app: demo-app
    app.kubernetes.io/name: demo-app
    env: staging
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8090
      targetPort: 8090
  selector:
    app: demo-app
